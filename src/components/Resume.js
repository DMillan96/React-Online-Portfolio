import TrackVisibility from "react-on-screen";
import { Container, Row, Col, Tab, Nav } from "react-bootstrap";

export const Resume = () => {
  return <section className="resume" id="resume"></section>;
};
